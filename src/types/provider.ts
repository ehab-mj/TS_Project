export type Provider = {
    id: string
    name: string
    category: string
    rating: number
    reviewsCount: number
    price: number
    location: string
    image: string
    description: string
    isAvailable: boolean
}